<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parcial3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetallesActivity" android:parentActivityName=".RecetasActivity" android:label="Detalles"></activity>
        <activity android:name=".FavoritasActivity" android:parentActivityName=".OpcionesActivity" android:label="Recetas Favoritas"/>
        <activity android:name=".AgregarActivity" android:parentActivityName=".OpcionesActivity" android:label="Agregar Recetas"/>
        <activity android:name=".OpcionesActivity" android:label="Menú"/>
        <activity android:name=".RecetasActivity" android:parentActivityName=".OpcionesActivity" android:label="Recetas Disponibles"/>
        <activity android:name=".MainActivity" android:label="Glovppetito42 Eats">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>